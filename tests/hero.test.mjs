import test from 'node:test';import assert from 'node:assert/strict';import{anticipatedShowcase}from'../lib/hero-data.mjs';
test('hero picks top ten dated upcoming games for this UTC year',()=>{const games=Array.from({length:14},(_,id)=>({id,title:'Game '+id,releaseDate:'2026-12-31',interest:id}));games.push({id:100,title:'Past',releaseDate:'2026-09-13',interest:900},{id:101,title:'Next year',releaseDate:'2027-01-01',interest:999},{id:102,title:'Unknown',releaseDate:null},{...games[13]});assert.deepEqual(anticipatedShowcase(games,new Date('2026-09-14')).map(g=>g.id),[13,12,11,10,9,8,7,6,5,4]);assert.equal(anticipatedShowcase([{id:1,title:'Today',releaseDate:'2026-12-31'}],new Date('2026-12-31T23:59:59Z')).length,1);assert.equal(anticipatedShowcase(games,new Date('2027-01-01')).length,1);assert.deepEqual(anticipatedShowcase(null),[]);});
